<h1><%= List.find(params[:list_id]).name %></h1>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.price %></td>
        <td><%= link_to 'Show', list_product_path(product.list_id, product.id), class: 'btn btn-secundary', role: :button  %></td>
        <td><%= link_to 'Edit', edit_list_product_path(product.list_id, product.id), class: 'btn btn-secundary', role: :button %></td>
        <td><%= link_to 'Destroy', list_product_path(@list.id, product), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_list_product_path, class: 'btn btn-primary', role: :button %>
